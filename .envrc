# vim: ft=sh

PATH_add examples
PATH_add bin

PATH_add .bats-prefix/libexec
PATH_add .bats-prefix/bin

test -f .envrc.local && source ".envrc.local"

if [[ $DEBUG -ne 1 && -f .envrc.debug ]]; then
  source .envrc.debug 
else
  source.envrc.no-debug
fi


