#!/usr/bin/env bash
# vim: ft=sh

( [[ -d ${BASHMATIC_HOME} ]] || bash -c "$(curl -fsSL https://bit.ly/bashmatic-install)" ) 2>&1 1>/dev/null

source "${BASHMATIC_HOME}/init.sh" 2>/dev/null

if is.a-function "$1"; then
  eval "$@"
fi

